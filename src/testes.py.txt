import jogovelha
import sys

erroInicializar = False
jogo = jogovelha.inicializar()

if len(jogo) != 3:
	erroinicializar = True
else:
	for linha in jogo:
		if len(linha) != 3:
			erroinicializar = True
		else:
			for elemneto in linha:
				if elemento != '.':
'					erroinicializar = true
if erro inicializar:
	sys.exit(1)
else:
	sys.exit(0)
